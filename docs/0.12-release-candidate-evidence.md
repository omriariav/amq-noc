# amq-noc 0.12.0 Release Candidate Evidence

Updated: 2026-06-28.

## Scope

Milestone `0.12.0` currently contains:

- #36: receipts/wake health includes delivery ladder and stuck nudge state.
- #45: clarify stale/historical needs-you so old or externally-satisfied asks do
  not read as current attention.
- #46: make the detail pane context-first and move repeated commands out of
  prime space.
- #47: audit updated from 2.9 to 2.10/2.11 contracts.
- #48: command audit demotes unsafe child actions in project-lead mode.
- #49: project control view consumes `coordination_boundary`, stays lead-first,
  and guards child actions.
- #50: consume amq-squad operator-loop leases and status contracts.
- #51: show operator directive backlog and lead conversation delivery state.

This candidate also retains the local release/task correlation work from the
mistaken higher-version workstream when it supports the v0.12.0 operator-loop
and control-surface scope.

## Local Candidate

Local implementation status:

- Named-profile task/runtime correlation now uses explicit `--project` and
  `--profile` arguments, so the NOC reads the correct task store for
  profile-scoped sessions such as `release-v0-12-0/v0-12-0`.
- Task/report mismatch rows and release evidence blockers are promoted into the
  JSON `attention_queue` with read-only inspect actions.
- Release evidence detection includes task titles and descriptions, but generic
  review or decision text does not create release blockers by itself.
- Documentation and install examples are retargeted for `v0.12.0` and the
  `amq-squad v2.10.0` companion.

Current release branch/PR state:

- Latest local release tag is `v0.11.0`; `origin/main` points at the `v0.11.0`
  merge commit.
- Local `main` is ahead of `origin/main` by one existing documentation commit
  (`fc977bd docs: require release milestone cleanup`).
- No release branch has been pushed.
- GitHub currently has no open PRs for this repository.
- `senior-dev` independently reviewed the local diff and approved it cleanly on
  AMQ thread `p2p/release-lead__senior-dev` at
  2026-06-28T20:37:17.07808Z.
- No merge, tag, GitHub release, or published install smoke has been performed.

## Verification

Local gates run successfully on 2026-06-28:

```sh
gofmt -l .
git diff --check
go test ./...
go vet ./...
make ci
```

Focused checks also passed:

```sh
go test ./internal/noc ./internal/cli
```

Live NOC smoke target for this workstream:

```sh
go run ./cmd/amq-noc noc --json --root /Users/omri.a/Code/amq-noc --filter session:v0-12-0
```

Observed smoke evidence:

- Profile `release-v0-12-0` resolves task store state for session `v0-12-0`.
- Task/report mismatch rows are visible when AMQ reports and task status
  disagree.
- Release evidence blockers are surfaced in `attention_queue` with read-only
  `threads` inspect actions.
- The live workstream row is `online`; a separate stale `codex-v0-12-0` profile
  row is historical context only.

## Remaining Gates

These remain intentionally blocked until the operator approves provider-side
effects:

- Commit and push a release branch.
- Open a PR and record the PR URL.
- Record PR URL and reviewed head SHA after a release branch/PR is approved.
- Merge the PR.
- Resolve milestone issues.
- Tag `v0.12.0`.
- Create the GitHub release.
- Run `make release-smoke VERSION=v0.12.0` against the published tag.
